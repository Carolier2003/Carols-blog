# Cloudflare Pages Functions Configuration
# This file helps configure the Functions deployment

[env.production]
name = "carols-blog"

# Specify the compatibility date for Workers runtime
compatibility_date = "2025-01-01"

# Enable Node.js compatibility for Buffer API
compatibility_flags = ["nodejs_compat"]
