---
import IconClock from "@/assets/icons/IconClock.svg";
import { formatReadingTime } from "@/utils/readingTime";

interface Props {
  minutes: number;
}

const { minutes } = Astro.props;
---

<span class="inline-flex items-center gap-x-2 text-sm opacity-80">
  <IconClock class="inline-block size-6 min-w-5.5" />
  <span>{formatReadingTime(minutes)}阅读</span>
</span>
